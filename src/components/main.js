import React, { Component } from 'react';
import Input from './input';

class Main extends Component {
    render() {
        return(
            <React.Fragment>
                <Input />
            </React.Fragment>
        )
    }
}

export default Main;